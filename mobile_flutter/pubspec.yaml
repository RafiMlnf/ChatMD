name: chatmd_mobile
description: ChatMD Mobile Client (Dart / Flutter) - Volatile Intranet Chat (Client Only)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  encrypt: ^5.0.3
  crypto: ^3.0.3
  shared_preferences: ^2.2.0
  file_picker: ^8.1.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
